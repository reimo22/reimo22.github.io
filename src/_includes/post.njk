{% extends "base.njk" %}
{% block content %}
  <article class="page">
    <header class="resume-header">
      <h1>{{ title }}</h1>
      <p class="resume-meta">{{ date | isoDate }}</p>
    </header>
    {{ content | safe }}
  </article>
{% endblock %}
